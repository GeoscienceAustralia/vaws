NOTE:

gsl/bin/*.dll must be in the PATH when windsim is run, one easy way to do this is to copy the dll's into the Python/DLLs folder.

The binaries for GSL where taken from here and relevant folders moved into source control:

http://gnuwin32.sourceforge.net/packages/gsl.htm